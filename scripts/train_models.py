# Example: import generators from preprocessing or ImageDataGenerator
# from custom_cnn_model import build_custom_cnn
# from transfer_learning_model import build_transfer_model

# Use your train_gen, valid_gen as defined in preprocessing/notebook

# model = build_custom_cnn()
# model.fit(train_gen, validation_data=valid_gen, epochs=30, callbacks=...)

# transfer_model = build_transfer_model()
# transfer_model.fit(train_gen, validation_data=valid_gen, epochs=20, callbacks=...)
